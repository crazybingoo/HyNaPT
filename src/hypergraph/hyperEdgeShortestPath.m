function pathLength = hyperEdgeShortestPath(firstEdge, secondEdge, distanceMatrix)
%HYPEREDGESHORTESTPATH Return a precomputed hyperedge shortest-path length.

pathLength = distanceMatrix(firstEdge, secondEdge);
end
