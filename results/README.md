# Generated results

This directory is reserved for local outputs. Its contents are ignored by Git to prevent accidental publication of participant-derived artifacts.

Do not place participant-identifiable or participant-level results here unless the repository itself is stored inside an approved controlled-access environment.
